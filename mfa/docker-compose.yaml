version: "3.9"
services:
  mfa:
    build:
      context: ./
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    volumes:
      - ./app:/app
      - ./app/cache:/root/.cache
    restart: "no"